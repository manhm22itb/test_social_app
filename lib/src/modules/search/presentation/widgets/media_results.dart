import 'package:flutter/material.dart';

class MediaResults extends StatelessWidget {
  const MediaResults({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Media results will appear here',
        style: TextStyle(color: Colors.grey),
      ),
    );
  }
}